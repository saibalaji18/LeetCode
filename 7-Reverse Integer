class Solution:
    def reverse(self, x: int) -> int:
        s = str(x)
        if s[0]=="-":
            s = s[:0:-1]
            x = -int(s)
        else:
            s = s[::-1]
            x = int(s)

        if x < -2**31 or x > 2**31 - 1:
            return 0

        return x
