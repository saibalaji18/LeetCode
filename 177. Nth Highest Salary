CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
BEGIN
  RETURN (
      # Write your MySQL query statement below.
      SELECT MAX(salary)
      FROM (SELECT salary, 
            DENSE_RANK() OVER (ORDER BY salary DESC) AS Salaryrank
            FROM 
            Employee) AS ranked_employee 
      WHERE Salaryrank = N

  );
END
